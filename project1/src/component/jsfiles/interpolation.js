let arr=[1,2,3,4]
console.log(`the nums are ${arr.map((val)=>{
// console.log(val)
  val
})}`);

console.log(`the nums are ${arr.reduce((acc,val)=>{
 return (acc+=val)
    })}`);
    
 
// console.log(`the nums are ${ }`);